spring:
  application:
    name: backend

  datasource:
    url: jdbc:sqlite:./backend/cinema.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        ddl-auto: create-drop

  mail:
    host: smtp.gmail.com
    port: 587
    username: ashvijhosdurg@gmail.com
    password: jfmrkslluulbtecg
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  level:
    org.springframework.jdbc.core: DEBUG

jasypt:
  encryptor:
    algorithm: PBEWithMD5AndDES
    password: Ják3i$C0ōŁ

